{
  "name": "YOLO26 Streamlit",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "customizations": {
    "vscode": {
      "settings": {
        "remote.restoreForwardedPorts": false
      }
    }
  },
  // Install from base image's package lists (no apt update for reproducibility)
  "postCreateCommand": "sudo apt-get install -y libgl1 && python -m pip install --upgrade pip && pip install -r requirements.txt"
}
