{
  "name": "YOLO26 Streamlit",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "customizations": {
    "vscode": {
      "settings": {
        "remote.restoreForwardedPorts": false
      }
    }
  },
  // - Update base image package index
  // - Install OpenGL dispatch layer package
  // - Update pip
  // - Install project Python dependencies
  "postCreateCommand": "sudo apt-get update -y && sudo apt-get install -y libgl1 && python -m pip install --upgrade pip && pip install -r requirements.txt"
}
